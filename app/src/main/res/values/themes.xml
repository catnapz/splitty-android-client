<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Splitty" parent="Theme.Material3.DynamicColors.DayNight">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- FirebaseAuth Theme -->
    <style name="Theme.Splitty.FirebaseAuth" parent="Base.Theme.Splitty">
        <item name="android:statusBarColor">@color/fui_transparent</item>
    </style>

    <style name="Theme.Splitty" parent="Base.Theme.Splitty" />
</resources>